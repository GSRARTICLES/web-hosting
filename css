:root {
  --bg: #08090c;
  --surface: #111318;
  --surface2: #181b22;
  --text: #f7f8fb;
  --muted: #a9b0bd;
  --line: rgba(255, 255, 255, .13);
  --accent: #d71920;
  --accent2: #ff4048;
  --white: #fff;
  --radius: 8px;
  --max: 1180px;
  --shadow: 0 20px 55px rgba(0, 0, 0, .35);
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  color: var(--text);
  background: var(--bg);
  font-family: Inter, Arial, sans-serif;
  line-height: 1.7;
  -webkit-font-smoothing: antialiased;
}

a {
  color: inherit;
  text-decoration: none;
  transition: .2s ease;
}

a:hover {
  color: var(--accent2);
}

img {
  display: block;
  max-width: 100%;
  height: auto;
}

button,
input {
  font: inherit;
}

.container {
  width: min(100% - 32px, var(--max));
  margin-inline: auto;
}

.skip-link {
  position: fixed;
  left: 16px;
  top: 16px;
  z-index: 9999;
  padding: 10px 14px;
  color: var(--white);
  background: var(--accent);
  border-radius: var(--radius);
  transform: translateY(-160%);
}

.skip-link:focus {
  transform: translateY(0);
}

.ethics-bar {
  background: #050608;
  border-bottom: 1px solid var(--line);
  color: var(--muted);
  font-size: 13px;
}

.ethics-bar .container {
  min-height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.ethics-bar strong {
  color: var(--white);
}

.site-header {
  position: sticky;
  top: 0;
  z-index: 999;
  background: rgba(8, 9, 12, .93);
  border-bottom: 1px solid var(--line);
  backdrop-filter: blur(16px);
}

.header-inner {
  min-height: 76px;
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 18px;
  align-items: center;
}

.brand {
  display: grid;
  line-height: 1.15;
}

.brand-title {
  color: var(--white);
  font-size: clamp(20px, 3vw, 30px);
  font-weight: 800;
}

.brand-description {
  margin-top: 4px;
  color: var(--muted);
  font-size: 13px;
}

.menu-toggle {
  display: none;
  padding: 9px 12px;
  color: var(--text);
  background: transparent;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  cursor: pointer;
}

.nav-shell {
  justify-self: end;
}

.nav-shell ul {
  display: flex;
  gap: 8px;
  align-items: center;
  justify-content: flex-end;
  margin: 0;
  padding: 0;
  list-style: none;
}

.nav-shell a {
  display: inline-flex;
  align-items: center;
  min-height: 40px;
  padding: 8px 12px;
  color: var(--muted);
  border-radius: var(--radius);
  font-size: 14px;
  font-weight: 700;
}

.nav-shell a:hover {
  color: var(--white);
  background: rgba(215, 25, 32, .15);
}

.search-open,
.back-to-top {
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--surface2);
  color: var(--text);
  cursor: pointer;
}

.search-open {
  width: 42px;
  height: 42px;
}

.hero {
  padding: clamp(52px, 8vw, 104px) 0 clamp(36px, 6vw, 70px);
  border-bottom: 1px solid var(--line);
  background:
    linear-gradient(90deg, rgba(8, 9, 12, .98), rgba(8, 9, 12, .82) 58%, rgba(215, 25, 32, .18)),
    url("https://images.unsplash.com/photo-1495020689067-958852a7765e?auto=format&fit=crop&w=1800&q=80") center / cover no-repeat;
}

.hero-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(280px, .8fr);
  gap: clamp(28px, 5vw, 58px);
  align-items: end;
}

.eyebrow {
  display: inline-flex;
  gap: 8px;
  align-items: center;
  margin-bottom: 18px;
  color: var(--accent2);
  text-transform: uppercase;
  letter-spacing: .08em;
  font-size: 12px;
  font-weight: 800;
}

.eyebrow::before {
  content: "";
  width: 28px;
  height: 2px;
  background: var(--accent);
}

.hero h1 {
  margin: 0;
  max-width: 840px;
  color: var(--white);
  font-size: clamp(38px, 7vw, 76px);
  line-height: 1;
  letter-spacing: 0;
}

.hero p {
  max-width: 690px;
  margin: 22px 0 0;
  color: #d9dde6;
  font-size: clamp(16px, 2vw, 20px);
}

.hero-card,
.post-card,
.blog-posts .post,
.sidebar .widget {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius);
}

.hero-card {
  padding: 22px;
  background: rgba(17, 19, 24, .88);
  box-shadow: var(--shadow);
}

.hero-card h2 {
  margin: 0 0 10px;
  font-size: 18px;
}

.hero-card p {
  margin: 0 0 18px;
  color: var(--muted);
  font-size: 14px;
}

.cta-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 28px;
}

.btn,
.read-more {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 10px 16px;
  border-radius: var(--radius);
  font-weight: 800;
}

.btn-primary,
.read-more {
  color: var(--white);
  background: var(--accent);
}

.btn-primary:hover,
.read-more:hover {
  color: var(--white);
  background: var(--accent2);
}

.btn-secondary {
  color: var(--white);
  border: 1px solid var(--line);
  background: rgba(255, 255, 255, .05);
}

.ticker {
  border-block: 1px solid var(--line);
  background: var(--surface);
}

.ticker .container {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 16px;
  align-items: center;
  min-height: 48px;
}

.ticker p {
  margin: 0;
  color: var(--muted);
  font-size: 14px;
}

.ticker-label {
  padding: 6px 10px;
  color: var(--white);
  background: var(--accent);
  border-radius: var(--radius);
  font-size: 12px;
  font-weight: 800;
  text-transform: uppercase;
}

.page-area {
  padding: clamp(34px, 6vw, 70px) 0;
}

.layout-grid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 340px;
  gap: 34px;
  align-items: start;
}

.section-title {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: 0 0 18px;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--line);
}

.section-title h2,
.widget h2,
.footer-widget h2 {
  margin: 0;
  color: var(--white);
  font-size: 18px;
  line-height: 1.25;
  font-weight: 800;
}

.featured-section {
  margin-bottom: 34px;
}

.post-card,
.blog-posts .post {
  overflow: hidden;
  animation: fadeUp .45s ease both;
}

.post-content-wrap {
  padding: clamp(20px, 3vw, 30px);
}

.post-card h3 {
  margin: 10px 0;
  font-size: clamp(24px, 4vw, 38px);
  line-height: 1.12;
}

.post-summary,
.post-snippet {
  color: var(--muted);
}

.blog-posts {
  display: grid;
  gap: 22px;
}

.blog-posts .post {
  display: grid;
  grid-template-columns: minmax(220px, .42fr) minmax(0, .58fr);
}

.post-thumbnail,
.post-thumb {
  min-height: 230px;
  background: var(--surface2);
}

.post-thumbnail img,
.post-thumb img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.index-post-title {
  margin: 0 0 12px;
  font-size: clamp(22px, 3vw, 30px);
  line-height: 1.15;
}

.post-body-wrap {
  padding: clamp(20px, 3vw, 30px);
}

.post-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 10px 16px;
  margin: 0 0 14px;
  color: var(--muted);
  font-size: 13px;
  font-weight: 600;
}

.post-labels a {
  display: inline-flex;
  margin: 0 6px 6px 0;
  padding: 3px 8px;
  color: var(--muted);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  font-size: 12px;
}

.post-labels a:hover {
  color: var(--white);
  border-color: var(--accent);
}

.blog-pager {
  display: flex;
  gap: 10px;
  justify-content: center;
  margin-top: 30px;
}

.blog-pager a {
  display: inline-flex;
  padding: 9px 12px;
  color: var(--muted);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--surface);
  font-weight: 700;
}

.sidebar {
  position: sticky;
  top: 100px;
  display: grid;
  gap: 18px;
}

.sidebar .widget {
  margin: 0;
  padding: 18px;
}

.sidebar p {
  color: var(--muted);
}

.sidebar ul,
.footer-widget ul {
  margin: 0;
  padding: 0;
  list-style: none;
}

.sidebar li {
  padding: 9px 0;
  border-bottom: 1px solid var(--line);
}

.sidebar li:last-child {
  border-bottom: 0;
}

.sidebar a,
.footer-widget a {
  color: var(--muted);
  font-weight: 700;
}

.sidebar a:hover,
.footer-widget a:hover {
  color: var(--accent2);
}

.child-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: #e63946;
  color: #fff;
  padding: 12px 18px;
  border-radius: 8px;
  font-size: 16px;
  font-weight: 700;
}

.search-overlay {
  position: fixed;
  inset: 0;
  z-index: 1000;
  display: none;
  align-items: start;
  justify-content: center;
  padding: 92px 16px 16px;
  background: rgba(0, 0, 0, .74);
}

.search-overlay.is-open {
  display: flex;
}

.search-panel {
  width: min(100%, 680px);
  padding: 18px;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}

.search-panel h2 {
  margin-top: 0;
}

.search-panel form {
  display: grid;
  grid-template-columns: 1fr auto auto;
  gap: 10px;
}

.search-panel input {
  min-width: 0;
  padding: 12px;
  color: var(--text);
  background: #090b10;
  border: 1px solid var(--line);
  border-radius: var(--radius);
}

.search-panel button {
  padding: 10px 14px;
  color: var(--white);
  background: var(--accent);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  cursor: pointer;
}

.search-panel .close-search {
  background: var(--surface2);
}

.search-note {
  margin: 12px 0 0;
  color: var(--muted);
  font-size: 13px;
}

.site-footer {
  padding: 42px 0 20px;
  background: #050608;
  border-top: 1px solid var(--line);
}

.footer-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 22px;
}

.footer-widget {
  margin: 0;
  color: var(--muted);
}

.footer-widget p {
  margin: 12px 0 0;
}

.copyright {
  margin-top: 30px;
  padding-top: 18px;
  color: var(--muted);
  border-top: 1px solid var(--line);
  font-size: 13px;
  text-align: center;
}

.back-to-top {
  position: fixed;
  right: 18px;
  bottom: 18px;
  z-index: 900;
  width: 44px;
  height: 44px;
  opacity: 0;
  pointer-events: none;
  transform: translateY(10px);
}

.back-to-top.is-visible {
  opacity: 1;
  pointer-events: auto;
  transform: translateY(0);
}

.is-hidden {
  display: none !important;
}

@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(12px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (max-width: 980px) {
  .header-inner {
    grid-template-columns: auto auto auto;
  }

  .menu-toggle {
    display: inline-flex;
    justify-self: end;
  }

  .nav-shell {
    grid-column: 1 / -1;
    display: none;
    width: 100%;
    padding-bottom: 16px;
  }

  .nav-shell.is-open {
    display: block;
  }

  .nav-shell ul {
    display: grid;
    justify-content: stretch;
  }

  .nav-shell a {
    width: 100%;
  }

  .hero-grid,
  .layout-grid {
    grid-template-columns: 1fr;
  }

  .sidebar {
    position: static;
  }

  .blog-posts .post {
    grid-template-columns: 1fr;
  }

  .footer-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 620px) {
  .container {
    width: min(100% - 24px, var(--max));
  }

  .ethics-bar .container {
    justify-content: start;
    text-align: left;
  }

  .header-inner {
    min-height: 66px;
    gap: 10px;
  }

  .hero {
    padding-top: 42px;
  }

  .ticker .container {
    grid-template-columns: 1fr;
    gap: 8px;
    padding-block: 12px;
  }

  .search-panel form {
    grid-template-columns: 1fr;
  }

  .footer-grid {
    grid-template-columns: 1fr;
  }
}
